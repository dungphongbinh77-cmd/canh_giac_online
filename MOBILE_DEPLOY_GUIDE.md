# HƯỚNG DẪN NHANH – ĐIỆN THOẠI

## 1. Kiểm thử trên máy tính

```bash
python -m streamlit run app.py
```

Mở `http://localhost:8501`.

## 2. Đưa lên GitHub

Repository cần có:

- `app.py`
- `requirements.txt`
- `data/`
- `model/`
- `modules/`

Đảm bảo `model/classifier.joblib` có trong repository.

## 3. Streamlit Community Cloud

- Đăng nhập bằng GitHub.
- Create app.
- Chọn repository.
- Branch: `main`.
- Main file path: `app.py`.
- Deploy.

## 4. Điện thoại

Mở URL `https://...streamlit.app` bằng Chrome/Safari.
Có thể dùng chức năng **Add to Home Screen** để tạo biểu tượng ngoài màn hình.

## 5. Mã học sinh

Dùng mã ẩn danh, ví dụ:

- `9A_HS01`
- `9A_HS02`
- `9B_HS01`

Không dùng tên thật.
